@using MvcResume.Models.Entity
@model List<TblSosyalMedya>

<div class="social-icons">
    @foreach (var x in Model)
    {
        <a class="social-icon" href="#!"><i class="@x.Icon"></i></a>
    }
</div>
